# 11 August 2026

`[x]` done · `[ ]` open · `[~]` deliberately held · `↳` an issue hit and fixed

---

## Open at end of day — carried to 12 August

- [ ] **Resend DNS** — 3 records at Hostinger; no mail leaves until they land
- [ ] **`storeOTP: "hashed"`** — the same hour the DNS lands; plain is a live
      credential sitting in a readable row
- [ ] **`VAPID_SUBJECT`** — still `mailto:REPLACE@example.org`, so push is
      untested as well as unset
- [~] **Delete D1** — held a few days on purpose; it is the rollback
- [ ] **The other four kinds** end to end — places, stays, start points, e-rickshaw
- [ ] **How values are stored** across the forms, reviewed as a whole
- [ ] **UI pass, round two** — needs eyes on it; I cannot see what I changed

## Next — 12 August

Agreed at the end of today. Everything above carries forward; these are the new
ones.

- [ ] **Admin gallery** — a place carries up to three photographs and the
      dashboard manages them one at a time. Ordering, replacing and removing
      within a record, not just adding
- [ ] **Places management** — the same pass the event form got today: sections,
      one map, what is actually being asked on each step
- [ ] **UI restructuring** — the dashboard as a whole rather than one form at a
      time

---

## 1 · D1 → Neon Postgres `[x]`

- [x] **Schema** — 4 migrations, run by `db/migrate.mjs`, each file in a transaction
- [x] **Auth tables regenerated** by the Better Auth CLI, not hand-translated
      → `emailVerified` became a real boolean, dates became `timestamptz`
- [x] **`store.neon.ts`** replaces `store.d1.ts` — still the only file with SQL
      → `?`→`$1`, `INSERT OR IGNORE`→`ON CONFLICT DO NOTHING`, `n=n+1`→`n=rev.n+1`
- [x] **Two drivers, one dependency** — `neon()` HTTP for the store, `Pool` for auth
      → auth needs interactive transactions; the HTTP endpoint will not do them
- [x] **Copy 103 rows** from the snapshot, one transaction, sessions dropped
- [x] **Verify locally** — feeds, ETag 304, sign-up, sign-in, admin gate, write round trip
- [x] **Deploy** — 5 secrets set, Worker deployed 09:23, app rebuilt
- [x] **Verify production** — all 6 feeds off Neon, `/img/` 200, admin 403, sign-up writes a code
- [~] **Delete D1** — 475 KB, not billing, and the rollback that does not live on one laptop
- [x] **Dead code out** — `store.d1.ts`, `migrations/`, `inspect-db.mjs`, `seed.sql`, `kysely-d1`

  ↳ **Local D1 was a migration behind** → every `/content/*.json` was a 500,
  `no such table: rev`. One missing migration takes out every feed at once.
  ↳ **`audit.id` would have collided.** Sequences ignore ids inserted past them,
  so the copy resets it — otherwise the *audit* insert fails after the edit lands.
  ↳ **`git stash` ate 10 tracked docs** mid-run. Restored from HEAD; do not stash
  a working tree full of untracked new files to run one typecheck.

---

## 2 · App / database / admin in sync `[ ]`

- [x] **Events checked end to end** — admin list ↔ `content/events.json` ↔ the app rail
      → already in sync; nothing to fix, which is why the work moved to the form
- [x] **Groundwork** — one database, one dev command, the app now reads the local API
- [ ] **Walk the other four kinds** end to end — places, stays, start points, e-rickshaw
- [ ] **Review how values are stored** across the forms as a whole

  ↳ **Local and production content had drifted** — 2 hotels there, 0 here.
  One database ends that class of bug rather than fixing this instance of it.

### 2a · Admin — the event form `[x]`

- [x] **Change a picture from the list** — the thumbnail is a button now
      → picker, upload and remove, saving only `img`; no form, no 20 fields to walk past
- [x] **Steps are cards** with a real heading and a line saying what the step is for
- [x] **One map, not two** — the route draws on the places map, which already had
      the tagged pins. Two maps of one district asked one question across both
- [x] **The corridor only exists for a yatra or a closure** — a festival at five
      temples has no road connecting them, and the field invited a line that
      looks like a road and is not one
- [x] **Conditional fields** — `when:{k,is}` on a spec, `applyWhen()` does the hiding.
      Hidden keeps its value: a mis-clicked dropdown must not delete somebody's work
- [x] **Layer switch is one word** — Leaflet's `collapsed:false` panel covered a
      quarter of every small map, in the corner routes run through
- [x] **The picker does not scope events to a folder** — `janmashtami-2026` will
      never have one, and the banner usually lives in the *place's* folder anyway
- [x] **Picker empty state is a block**, not a paragraph and a slab laid out as two pictures
- [x] **Places field takes the full row** — it holds chips, a search box and a map,
      and was sharing a row with a two-option dropdown

  ↳ **The merge shipped broken and looked right in the code.** `initRefs` never
  set `host._map`, so the corridor could not find the places map and silently
  built a second one. Only a screenshot showed it.
  ↳ **A borrowed map outlives the field that drew on it.** Change kind from yatra
  to festival and the click handler stays bound — every click would add a route
  point to a festival. Guarded on `fld.hidden`, at wire time and at click.
  ↳ **A backtick in a comment ends the `String.raw` block.** Three times today.
  `check-forms.mjs` caught every one.

### 2b · User side — event and place pages `[x]`

- [x] **Event detail page** — `/event/:id`. The home card opens the event, not
      the planner: someone reading "in 24 days" is deciding *whether* to come
- [x] **Tagged places as a stacked decision list** — `Pcard`, which carries
      distance and open/shut. A rail hides options 3–5 behind a swipe, and
      comparing two of them is the only thing the reader is there to do
- [x] **No invented route between tagged places** — five temples are not a
      journey. The planner already computes the real order from the road matrix
- [x] **"Where it happens" is set apart** — its own ground, a brass count in the
      heading. It is the one place on the page that asks for a choice
- [x] **The notice is a panel**, and `avoid` takes the shut colour — "keep away
      from this road" and "worth going to" must not arrive in the same beige box
- [x] **Place photographs are swipeable** — 27 of 57 places carry a gallery and
      the page showed the first and dropped the rest. In the hero, not a strip
      below it: the pictures are how you decide a place is worth an hour
- [x] **No auto-advance on a detail hero** — `useRail(…, 0)`. The home rails are
      ambient; here the reader chose this place and is looking at this picture
- [x] **"Close by" is a stacked list too** — same reasoning as the event page
- [ ] **UI pass, round two** — needs eyes on it; I cannot see what I changed

  ↳ **`.ph` was an INLINE box.** `Photo` renders a span and nothing set
  `display`, so every `.ph{aspect-ratio}` in the stylesheet was inert — a
  portrait photo rendered full-width and hundreds of pixels tall, pushing the
  name off the card. It read as "the card has no text". One line fixed every
  `Fcard` in the app. `.pcard` escaped only because flex blockifies its children.
  ↳ **`.sec-head` is a flex row built for "heading … link".** Putting a
  paragraph in it made a second column, squeezing the heading into a stack with
  a full-size icon on top. Not every wrapper class is a generic container.
  ↳ **`Fcard` was the wrong card** — built for browsing rails. `Pcard` already
  existed for choosing, and carries the two facts that decide it: how far, and
  whether it is open.
  ↳ **An event has no banner of its own.** All four fell back to their first
  place's photograph. Setting one now overrides it; removing it goes back to
  borrowing.

---

## 3 · One command to run everything `[x]`

- [x] **`npm run dev`** — `dev.mjs` starts Worker + app, labelled output, Ctrl-C kills both
- [x] **Reachable from a phone** — both bind every interface, app calls the API by LAN address
- [x] **`VITE_CONTENT_URL` set in dev** so the app actually calls the API
      → cost: those are live rows now. `web:dev` + `api:dev` gets the old behaviour back
- [x] **`trustedOrigins` takes a list** — localhost and the LAN address are both legitimate

  ↳ **Wrangler does not read Worker vars off the shell.** `dev.mjs` passed `APP_URL`
  as an env var; it looked like it worked and did nothing. `--var` now.
  ↳ Only caught by reading an actual email out of `.wrangler/tmp` — an earlier
  "both origins return 200" check had given the same variable a false pass.

---

## 4 · Drizzle removed `[x]`

- [x] **Gone** — `drizzle-kit`, `drizzle-orm`, `@libsql/client`, config, `drizzle/`, 4 scripts
      → it was a viewer, kept because looking at D1 was awkward; the Neon console is not
- [x] **`npm run inspect` gone too** — the migration runner's transaction is the same
      guarantee, obtained honestly rather than by looking first

---

## 5 · Email verification + password reset `[x]`

- [x] **`src/email.ts`** — sender and templates, English and Hindi in every message
      → Better Auth hands over a user row with no language on it
- [x] **Verification required** — sign-up returns `token: null`, sign-in 403 until confirmed
- [x] **Migration 0005** marks the 2 pre-existing accounts verified, scoped by `createdAt`
      → without it, deploying the flag locks the Board out of its own dashboard
- [x] **A failed send never fails the account behind it** — logged with its error code
- [x] **Neither form reveals whether an address is registered** — reset answers 200 either way
- [x] **Reset screen** — token out of the URL and stripped with `replaceState`
- [x] **Verified** — 403 unverified · 302 on the link · 400 on reuse · 401 on the old password

---

## 6 · Mail is configuration, not code `[x]`

- [x] **4 variables** — `EMAIL_PROVIDER` / `_FROM` / `_NAME` / `RESEND_API_KEY`
      → `deliver()` is the only function that has heard of a provider
- [x] **Three providers** — `cloudflare`, `resend`, `log`; verified by watching `log` fire
- [x] **Six-digit code** instead of a link, via the emailOTP plugin
- [x] **`npm run otp`** reads it out of `verification` — testable with no mailbox at all
- [x] **Code screen in the app** — numeric, `one-time-code` autofill, resend button
      → an unverified sign-in routes there with a fresh code, not an error message

  ↳ **`sendVerificationOnSignUp` and `overrideDefaultEmailVerification` are mutually
  exclusive.** Set both and nothing sends: 200, no attempt, empty table, no warning.
  ↳ Found by checking the table rather than trusting the status code.

### 6b · Resend `[x]` — domain `[ ]`

- [x] **Switched providers** — three variables, no code touched. The point of task 6
- [x] **Cloudflare Email Sending abandoned** — `2036 Unauthorized` on every credential
- [ ] **`kkr.indietribe.space` PENDING** — 3 records at Hostinger, which is not Cloudflare
- [x] **`npm run email:status`** — records, what is pending, `--verify`, and a real test send

### 6c · One `.env` `[x]`

- [x] **`.dev.vars` deleted** — wrangler reads a plain `.env` since 4.x, so one file
- [x] **`--env-file` on `dev` only**, never `deploy` — local values must not travel up
- [x] **Every variable commented** — what it does, what breaks without it, where production's lives

### 6d · One copy of the secrets `[~]`

- [x] **`VAPID_PUBLIC` recovered** — from the Worker's own `GET /vapid`, not the secret store
- [x] **The other two cannot be** — Worker secrets are write-only; that is the point of them
- [x] **`npm run secrets:push`** — uploads only real credentials, refuses placeholders
      → never the Cloudflare token, never things that belong in `[vars]` and would shadow them

  ↳ **`EMAIL_FROM` had a leading space.** `node --env-file` keeps it, so the address
  was `" noreply@…"` and Resend would have blamed the address, not the space.
  ↳ **`AUTH_SECRET` was still `local-dev-only-replace-me`** — a string that is in git.
  In production that is not a weak secret, it is a published one. Guarded now.

---

## 7 · R2 ↔ database `[x]`

- [x] **Production was never broken** — 99 objects, 99 content refs, all serving 200
- [x] **Local dev was** — `wrangler dev` binds an empty emulated bucket; `remote = true` fixes it
      → cost: an upload from a laptop is now an upload in production, as the database already was

  ↳ Collateral from the Neon move: the database became real while the bucket stayed
  a copy, so the dashboard listed 99 photographs against a bucket holding none.

---

## Also `[x]`

- [x] **Deleted docs 06 and 07** — a palette the code replaced, and 1567 lines of specs
      written against a branch that no longer exists. `global.css` is the design system
- [x] **Cleared `TASKS.md`**, old items kept under "Deprecated" as a record, not a commitment
- [x] **Indexed the repo** into the codebase graph — 1419 nodes, 4613 edges
- [x] **`docs/16`** — the cut-over deploy, step by step, with the rollback

  ↳ **`wrangler` auto-loads `apps/api/.env`**, so the token there shadows your browser
  login for any command run from that directory. It can deploy; it cannot touch DNS.
  ↳ That shadowing is what made `wrangler email sending` look like a permissions problem.
